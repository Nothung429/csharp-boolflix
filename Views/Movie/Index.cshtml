@{
    Layout = "_Layout";
}

<div style="width:1500px; margin:auto">
    <h1 class="text-center display-4 mt-3">Welcome to Pizzeria Totò Sapore</h1>
    <table class="table table-dark table-striped table-hover mt-4">
        <thead>
            <tr>
                <th scope="col">Immagine</th>
                <th scope="col">Nome</th>
                <th scope="col">Prezzo</th>
                <th scope="col">Categoria</th>
                <th scope="col">Descrizione</th>
                <th scope="col">Ingredienti</th>
                <th scope="col">Azioni</th>
            </tr>
        </thead>
        <tbody>
            @*@foreach (Movie movie in Model)
            {
                <tr>
                    <td>
                        <img width="100" height="100" src="@movie.Image" class="rounded-circle">
                    </td>
                    <td>
                        <h3>@movie.Name</h3>
                    </td>
                    <td>
                        <h4>@movie.Price€</h4>
                    </td>
                    <td>
                        <h4 class="badge bg-success">@movie.Category.Name</h4>
                    </td>
                    <td>
                        <p>@movie.Description</p>
                    </td>
                    <td>
                        @if (movie.Ingredients != null)
                        {
                            @foreach (Ingredient ingredient in movie.Ingredients)
                            {
                                <h4 class="badge bg-warning">@ingredient.Name</h4>
                            }
                        }
                    </td>
                    <td>
                        <a href="@Url.Action("Show", "Pizza", new {Id = movie.Id})" class="btn btn-outline-info">
                            Visualizza
                        </a>
                        <a href="@Url.Action("Edit", "Pizza", new {Id = movie.Id})" class="btn btn-outline-warning">
                            Modifica
                        </a>
                        <form class="d-inline" asp-controller="Pizza" asp-action="Delete" asp-route-id="@movie.Id" method="post">
                            <button type="submit" class="btn btn-outline-danger">
                                Cancella
                            </button>
                        </form>
                    </td>
                </tr>
            }*@
        </tbody>
    </table>
</div>
